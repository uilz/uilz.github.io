# Node 日志
*.log

# 操作系统生成的文件
Thumbs.db
.DS_Store

# 编辑器配置（可根据你使用的编辑器选择）
.vscode/
.idea/
*.swp
*.swo

# 其他项目可能残留的临时文件
*.tmp

# banji 依赖与构建中间产物
node_modules/
apps/banji/dist/
# 注意：i/banji/ 是 vite 构建产物，但本仓库无 CI，Pages 直接 serve 仓库文件，
# 因此 i/banji/ 必须入库（发布 = npm run build && git add i/banji && push）。
