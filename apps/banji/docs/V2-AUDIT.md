# 伴记 · V2 产品审计地图（真浏览器全旅程 + 23 截图 + DOM 探针）

> V2 开轮由一次真人感的实机审计发起：完整走一遍「翻历 → 落笔 → 回看 → 搜索 → 设置」，
> 390/768/1100 三档屏宽截图与 DOM 探针留证（截图档已随 /tmp 散失，下列每条都有探针记录背书）。
> 本文件是 V2 各轮的工作账本：未来会话不必重跑审计，从这里接手。

## 问题清单（按优先级）

### P0 —— 每日循环的断点

| # | 症状 | 探针证据 |
|---|---|---|
| 1 | 移动端日页眉破版：390px 日期折行、与模式开关碰撞 | 390 截图 header 两行折实锤（boundingBox 实测换行） |
| 2 | 时间体验断：日视图无上一/下一天；过去日无纵深；跨月无连续感 | DOM 探针证实日视图无任何 prev/next 按钮 |
| 3 | ~~第一眼=Calendar UI：无最近落笔、无当天状态、无跨月连续——「这是我的时间」不成立~~ **V2-Iter2 销**：今日面书脊三态（今天·N张纸/上次落笔一行/新册空着）+ 时间账「此册已记 N 日 · 最近 M月D日 · 该月 N 日」 | 首屏截图：与任意月历应用无异 |
| 4 | ~~卡片没有身份：文字卡同貌白矩形；图模式 48 枚全等 chip；型间无视觉语言~~ **V2-Iter3 销**：纸的方言十型材质进框（data-paper→paper.css 一选择器），图模式 chip 材质色+角印（chipMaterial 纯函数）；DESIGN.md 立典 | 图模式截图整排同质矩形 |
| 5 | 保存无反馈：落笔后零信号 | DOM 探针：落笔后无任何 saved 信号元素 |

### P1 —— 手感与纵深

| # | 症状 |
|---|---|
| 6 | ~~墨点三档无区分度（24 天种子数据在当月月历仅 6 枚针尖墨点，密度读不出来）~~ **V2-Iter2 销**：s/m/l=3/4.5/6px·α.5/.7/.9 令牌分档 + 贴照日 6px 发丝折角（e2e boundingBox 实测极差 ≥2） |
| 7 | 图模式=文件柜：顶对齐、75% 空白、关系线存在感≈0 |
| 8 | 线模式单卡空态：一颗珠子挂在大空页上 |
| 9 | 搜索结果纯文字列表：无类型、无上下文、无预览 |
| 10 | ~~纸面/页底两段色带接缝：底部动作条漂在灰带上~~ **V2-Iter3 销**：画布 flex 长到滚动窗底缘（1100/390 真 rect e2e 钉），底栏浮纸上以 radial 光晕代替硬灰带 |
| 11 | ⋯ 菜单无复制/多选/折叠 |
| 12 | 卡片固定 300×150：文字不贴内容，短纸空、长纸裁 |
| 13 | 无 redo；undo 仅「删除后 10 秒单级」 |
| 14 | ~~回看缺「任意一天」的门（只能一格一格翻）~~ **V2-Iter2 销**：「翻到」月题之门（当年 12 格+年份 ‹ ›+回到今天，点月直达）；到月之后每一格本就是「任意一天」，门+格合成全径 |

### P2 —— 装饰与仪式

| # | 症状 |
|---|---|
| 15 | ~~模式开关方框描边最「dashboard」~~ **V2-Iter3 销**：段开关去框去垫——亮者自墨坠 2px 朱丝 underline、暗者淡墨无饰（`.bj-mode-seg-btn` 钩子一字不动，既有 `:has-text` 检原样绿） |
| 16 | ~~正文/手记格式 chip 过大~~ **V2-Iter3 销**：饼边退役、≤13px 小注、亮者自墨下划（手机 44px 热区法保留；切换行为未回退） |
| 17 | 空日耳语悬在巨空画布上 |
| 18 | 夜读墨点仍弱 |
| 19 | ~~拖拽无抬升语言~~ **V2-Iter3 销**：真拖 `is-lift`+`--bj-lift` 两层暖影+`scale(1.012)`+120ms，落手 180ms 沉回基线（e2e 真指针在途/落手双检） |
| 20 | 导出/导入缺克制仪式 |

## 轮次计划

- **Iter 1：#1 #2 #5 #12** —— 日常循环舒适轮：页眉不破版、日子能翻、落笔有声、纸贴内容。（已交付）
- **Iter 2：#3 #6 #14** —— 首页重设计（「这是我的时间」的第一眼）：今日面书脊、时间账、墨点信息量+折角、翻到之门。（已交付）
- **Iter 3：#4 #10 #19** —— 卡片视觉语言（型间方言、接缝、抬升）+ 设计系统立典 DESIGN.md + P2 #15 #16 消化。（已交付）
- Iter 4：#7 #8 #9 回看与关系（图、线、搜索；#14 已由 Iter2 先行兑现）。
- Iter 5：#11 #13 编辑纵深（菜单、redo）。
- P2 随轮消化。

## 证据注记

- DOM 探针：日视图无任何 prev/next 按钮；落笔后无任何 saved 信号元素。
- 390px header 两行折实锤（日期整串 + 模式开关挤不进一行）。
- 24 天种子数据在当月月历仅 6 枚针尖墨点（三档密度读不出差别）。
